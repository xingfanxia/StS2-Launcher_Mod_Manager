# Launcher stability failure matrix

Evidence snapshot: 2026-08-15. Device logs are kept outside git at
`~/Library/Caches/StS2LauncherInvestigation/upstream-logs/`; proprietary game
assemblies and APK inputs are likewise excluded. Status vocabulary is restricted
to the completion states required by `GOAL.md`.

| Stage / visible symptom | Status | First causal evidence (not merely the last log line) | Launcher boundary and current handling | Remaining proof |
|---|---|---|---|---|
| Before launcher: process exits with little or no app log | instrumented-awaiting-evidence | The logger is a child `logcat --pid` process. A native crash, LMK, or ANR kills/stops the producer before its own fatal classification can be appended. | On Android 11+, a daemon worker on the next boot reports `ApplicationExitInfo`: reason, status, importance, PSS/RSS, description, and a bounded ANR trace. Planned launcher restarts are marked so `EXIT_SELF` is not reported as a crash; the query/trace never blocks `Activity.onCreate()`. The read-only device capture tool also preserves system exit-info before/after each matrix row. | Trigger managed crash, native crash, LMK, ANR, and planned restart on a physical device; verify one report per exit and no false crash for restart. API 36 emulator preflight confirms the query exists but is not device proof; Android 7–10 remain logcat-only. |
| Before launcher: PCK update cache invalidation freezes/ANRs | confirmed | `GodotApp.onCreate()` recursively deleted every entry below the active ETC2 cache on Android's UI thread. Issue #86's 722-entry cache happened to finish in 175 ms, but work was unbounded and the explanatory overlay was created only afterward. | Active cache directories are now renamed out of Godot's path, then recursively deleted on a daemon worker after `super.onCreate()`. Failed staging preserves the trigger/stamp for retry; abandoned staging directories are collected next boot. | Java fixtures prove atomic staging, background deletion, collision safety, and interrupted-cleanup discovery. A physical public-beta→public PCK change staged 428 entries, kept the Activity responsive, and rebuilt without ANR. |
| After PCK update: image-cache rebuild overlay never leaves and launcher appears unresponsive | reproduced | On the physical public-beta→public transition, the process stayed alive with 30 s heartbeats and active CPU, but the native overlay still swallowed every touch after launcher initialization. Source ordering showed `hideLoadingOverlay` had moved after `await launcher.WaitForLaunch()`, creating a cycle: PLAY was required to hide the view that blocked PLAY. | The overlay is now dismissed immediately after successful launcher initialization (and on initialization failure), before the PLAY wait. A source-order regression failed before the fix and passes after it. The signed device build logged `Launcher UI displayed` followed 1 ms later by `Loading overlay hidden`, then accepted PLAY. | None for this failure class; future changes must retain the ordering regression. |
| Before launcher: copying BCL/game assemblies is the observed ANR root | ruled out | Across the collected logs, all 186 BCL assets copy in roughly 0.15–0.2 s; game DLL sync immediately follows and no captured run contains `am_anr` at this stage. | Keep the existing size/mtime game-DLL synchronization because it prevents stale DLL/PCK pairs. Previous-exit telemetry will distinguish a future real startup ANR. | Re-evaluate only if `ApplicationExitInfo.REASON_ANR` trace points here on a slow device. |
| Launcher page collapses, disappears, or stops accepting input after resize/fold | confirmed | Prior device captures and existing issue fixes identify 0×0 parent sizing, unplanned tree teardown, stale viewport hooks, and game input handlers firing under the launcher. | Existing focused fixes set explicit size, pin/restore content scale, detach hooks, suppress underlying input, and emit a 30 s heartbeat plus planned/unplanned teardown marker. | Physical rotation/configuration restore plus 10 s HOME/resume kept the same PID, recreated the surface, recovered the full layout, and accepted input. Split-screen and external keyboard remain device-specific residual tests. |
| Controller glyphs all show the same button or controller input appears dead after PLAY | confirmed | Physical-device logs show `NInputManager.Init()` faulting after its controller strategy initializes but before it reads settings-backed maps. Decompiled v0.107.1 control flow confirms the fire-and-forget task can leave `_controllerInputMap` empty; this is non-terminal for touch and distinct from launcher teardown. | Existing issue #85 handling refills an empty controller map from the game strategy on the first controller-family notification. The async initializer is deliberately not retried wholesale because `NControllerManager.Init()` registers events and is not idempotent. | Connect a controller, verify the one-shot refill log, non-placeholder glyphs, and action dispatch after PLAY; keyboard remapping remains a separate unsupported Android edge unless reproduced. |
| Launcher UI construction fails, caller waits forever | reproduced | `LauncherUI.Initialize()` caught `BuildUI` exceptions and returned with `_model` potentially null; `RunLauncherThenGame` then called `WaitForLaunch()` anyway. The lifecycle source-contract test reproduces the unsafe control-flow contract. | `Initialize()` now returns an explicit result and hooks cleanup before changing Window state. In-game failure removes the partial UI and continues in local-safe game mode; standalone failure displays an `OS.Alert` instead of a blank screen. | Inject a deterministic Godot `BuildUI` failure on device and verify Window scale restoration, alert/bypass, and diagnostic log. |
| Android Back or parent teardown closes an awaited picker/dialog, then launcher action never finishes | reproduced | Five result dialogs, the branch picker, and `SimpleResultDialog` only completed their TCS/event from button handlers. `ModalGate.TryHandleBack()` calls `QueueFree()` directly, so Back bypassed those handlers; two dialogs were not registered with `ModalGate` at all. | A one-shot `DialogCompletion<T>` supplies a typed fallback from `TreeExiting`; all awaited dialogs now register Back handling and every button/teardown race has one winner. Branch and result events use equivalent one-shot teardown callbacks. | Device Back on every picker/conflict/result dialog plus parent teardown while each is open. |
| Whole launcher is removed before PLAY and patched game startup waits forever | reproduced | Existing device history already records unplanned launcher tree teardown during multi-window churn. `OnExitTree()` disposed the model, but `_launchTcs` and a pending Steam Guard `_codeTcs` were never completed. | `WaitForLaunch()` now returns a one-shot bool: PLAY=true, teardown=false. Dispose releases both launch/auth waits; game startup falls back to local-safe mode instead of hanging. Continuations run asynchronously. | Remove the launcher during fold/split-screen and during Steam Guard entry; verify false handoff, connection disposal, and game startup. |
| Back during the PLAY-to-game transition tears down the cloud UI and reaches a half-started game | confirmed | A physical-device log reaches planned launcher teardown, opens the cloud conflict path, then records `Dev console used before being created` from the underlying input managers. A deferred cloud progress callback subsequently touches a disposed `StyledLabel` and Godot shuts down. `LauncherUI.OnExitTree()` had already restored `AutoAcceptQuit=true` while the overlay still owned the display. | A reference-counted `StartupInputGate` now spans every cloud/no-cloud and warmup/no-warmup path through `GameStartup`. It suppresses underlying game input, keeps auto-quit disabled, routes Back only to the top modal, and validates every deferred overlay target before mutation. | On device, press Back during cache preload, conflict backup/apply, first warmup, and the no-cloud handoff; verify the top modal alone closes and startup neither quits nor faults. |
| Before launcher aborts in Godot `StringName`, or a mod resolves `ApplicationData` to `/data/.config` | confirmed | Two mods in earlier physical-device logs fail below `/data/.config`. The first attempted fix called `OS.GetUserDataDir()` from the unmanaged .NET entrypoint: an old/new APK A/B on the API 36 ARM64 device reached `GodotSharp bootstrapped`, then the new build emitted `_reset_state` and aborted in a destroyed mutex before its next log. Removing that early Godot call made the same signed build reach the launcher and remain alive. | `GodotApp` creates `.config` from Android's real per-user `getFilesDir()` and uses `android.system.Os.setenv` before native/.NET bootstrap. `ModEntry` only reads that value and has a managed assembly-directory fallback, so no Godot singleton is touched during `gd_mono` initialization. | Physical standalone, BaseLib-only, and ModConfig runs all used the app-private XDG directory, initialized, and reached the main menu with no `/data/.config` failure. |
| Background, quit, or cloud-conflict handling blocks the Godot main thread | confirmed | `EnterBackgroundMode` synchronously polled cloud writes for 5 s, `NGame.Quit` did so for up to 300 s, and both conflict-verification paths synchronously waited up to 300 s after a main-thread dialog choice. With slow or unreachable Steam, the UI/Activity lifecycle could not process frames or `onPause` promptly. | All cloud drains now run on workers; verification remains awaited with a bounded result, while quit marshals restart back to the main thread. Repeated lifecycle/quit callbacks are coalesced, and every scheduling/restart failure releases its one-shot latch so a later Quit can retry. | Offline Steam timeouts/reconnects and HOME/resume remained responsive and fell back locally within bounded attempts. Exact packet-level throttling was unavailable on the non-root device; queued-write quit under induced latency remains a residual stress case. |
| First shader warmup: black screen, crash around hundreds of scenes, or same restart every boot | reproduced | Two physical runs separated the causes. The original whole-graph collector was killed by Android LMK (`LOW_MEMORY`) before its first material-count log. A first streaming revision still used generic `Load<Material>` on unrelated `.tres` files, throwing before the mismatched wrapper could be disposed and creating an exception/leak storm that ended in another LMK. | Version 7 enumerates lightweight paths, loads base `Resource` with `IgnoreDeep`, disposes every mismatch and `PackedScene`, deduplicates immediately, and renders/releases batches of at most 8 materials. The attempt marker prevents a crash loop. A regression forbids the typed-load and whole-graph patterns. | On the ARM64 device it scanned 2,580 loose resources plus 947 scenes, streamed 1,592 materials in 156 s, peaked at about 1.94 GiB RSS with about 2.84 GiB system memory still available, completed/restarted, and the next PLAY skipped warmup. No LMK, fatal signal, or ANR occurred. |
| Shader warmup completion or initialization failure leaves permanent await | reproduced | `_tcs` was created only by `WaitForCompletion()`, after `Initialize()` could synchronously fail/complete. Success then called Java `restartApp()` and relied on `Runtime.exit()` to end the awaiting flow rather than completing it. | Completion now exists eagerly, state is persisted before signaling, the caller owns the clean-process restart, and a missing/returning Android bridge falls through instead of waiting forever. State-machine regression tests reproduce the old ordering. | The physical success/restart and warm-cache paths passed; automated tests cover early completion and interrupted-attempt recovery. Missing/returning bridge remains a test-double-only fallback. |
| Play pressed, then black before game startup | confirmed | Issue #86's first causal error is `TypeLoadException`: v0.111 added `MakeDirRecursive`/`CopyFile` to `IModManagerFileIo`; the old `ExternalModsFileIo` VTable could not load. Later `QueuePresentKHR` lines occur after Activity/surface teardown and are consequences. | Existing forward-compatible virtual members fix the known contract. `memberref-audit` now also checks every current abstract slot of implemented game interfaces, catching this class of break before runtime. | Public-beta build 24724944 / Windows manifest `5661886308395090154` ran on the ARM64 device: all patches applied, the v0.108+ save-path capability branch executed, ReleaseInfo identified v0.111.0, and main menu loaded in 38.38 s with no `TypeLoadException`, missing member, fatal signal, or ANR. Static audits also pass against both supported builds. |
| Game upgrade changes a directly called method/field signature | confirmed | Issues #42/#43/#53 and collected issue #53 logs show `MissingMethodException` such as the old `GetProgressPathForProfile(int)` reference after a game update. | MemberRef metadata audit covers every direct `sts2` call/field reference and current interface slot. The real APK build now runs it before Gradle packaging. | Public build 23811903 and public-beta build 24724944 both pass with 52 MemberRefs, 3 interfaces, and 0 missing. Repeat for any additionally declared supported branch before release. |
| Game upgrade changes a reflection/Harmony target or transpiler IL shape | confirmed | `GetMethod("...")`, `AccessTools`, and Harmony target names are strings and therefore absent from MemberRef metadata. The launcher also reflects private `GameStartup`; a missing target after launcher teardown would present as black. Past async lowering moved `ModManager.Initialize` logic into `MoveNext`, proving IL shape is unstable. | Independent `patch-target-audit` checks 59 required/optional target rules, bare-lookup ambiguity, and both calls expected by the `LoadProgress` transpiler; the APK build enforces it. | Public build 23811903 and both public-beta build 24724944 DLL copies pass with 0 required failures and 0 optional degradations. A required miss blocks release; an optional miss is accepted only with its explicit skip/log fallback. |
| Launcher/cloud work looks frozen after Play | confirmed | Issue #53 motivated the launcher→game overlay: cloud handshake/conflict/backup/apply can take one to two minutes. Collected logs show cache preload commonly takes 1.5–6.3 s and is bounded at 15 s. | Existing overlay reports cloud phases. Cache timeout/failure degrades to local-only for the session, preventing an unauthenticated/default cache from overwriting cloud data. | Physical online runs classified identical saves and reached the menu; a cloud-only conflict safely selected the cloud copy. Offline enumeration failed within bounded retries and launched from local data. Exact divergent two-sided conflict and rooted packet shaping remain destructive/specialized residual tests. |
| BaseLib/MonoMod/Harmony or a third-party mod causes managed/native failure | external/unsupported | Issue #87 contains repeated Godot `BUG: Unreferenced static string` corruption before silent process loss; issue #55 documents Mono Android DynamicMethod/init-setter failures. A launcher cannot safely repair arbitrary mod IL or native memory corruption. | Existing narrowly scoped compatibility patches, per-patch resilience, FMOD event guard, mod attribution, and safe mod disable/stash remain. Previous-exit classification exposes native/LMK/ANR terminal reason instead of an unexplained black screen. | Physical no-mod, BaseLib-only, supported ModConfig, and QuickRestart-without-BaseLib runs passed: the first three reached main menu; the incompatible set emitted its missing-dependency diagnosis and safely skipped loading. Reproducing issue #87's external native corruption still requires its smallest failing mod set. |
| Vulkan/Adreno initial render failure versus background teardown | instrumented-awaiting-evidence | Genuine renderer failures must occur before a usable surface/frame. In issue #86 logs, `QueuePresentKHR` appears only after Activity backgrounding/surface destruction, so it cannot explain the earlier managed `TypeLoadException`. Original launcher issue #55 contains a possible initial present failure but lacks equivalent current-build reproduction. | Device fingerprint, launcher heartbeat, Activity lifecycle, render diagnostics, and previous-exit reason are retained. No renderer is globally disabled and a teardown tail is not promoted to root cause. | Reproduce on the affected GPU; compare first surface/frame and exit reason, then test renderer fallback only on an equivalent fingerprint. |
| PCK and cached game assemblies belong to different game builds | confirmed | Issues #5/#42/#43/#44 show that an old early return left `sts2.dll` stale while a newly downloaded PCK was used. | Existing per-file size/mtime synchronization runs each boot while protecting Android Mono BCL files. Branch changes require a process restart; atlas cache stamp follows PCK mtime. | Physical public↔public-beta switches downloaded the selected manifest, forced a controlled process restart, recopied all matching game assemblies (32 for beta, 30 for public) while preserving 186 BCL files, invalidated the PCK texture stamp, and loaded the matching game version. Interrupted-download recovery remains a residual destructive test. |
| Java Unicode-extension locale causes permanent black during localization | confirmed | Issue #71's first causal managed exception is `CultureInfo` rejecting ROM output such as `ko_KR_#u-...`; later render/surface messages are not the cause. | Existing `PlatformPatches.SanitizeRawLanguagePostfix` strips Java/BCP-47 extension tails before `CultureInfo` construction. | Unit cases plus affected Lenovo/ZUI (or injected locale) device regression. |
| Harmony fails before launcher because Android has no usable TMPDIR | confirmed | Issues #19/#27 traced `HarmonySharedState`/MonoMod temp emission to `/tmp` on ROMs without `TMPDIR`. | Existing early `ModEntry.Apply` creates app-private cache/MonoMod and sets `TMPDIR` before Harmony work; stale temp files are bounded by age cleanup. | Affected ROM or launch with TMPDIR unset; verify all critical patch logs. |
| FMOD native crash at process load or mod-bank mismatch | confirmed | A signed device build failed before `onCreate` with FMOD JNI error 28. Native inventories proved all 19 `.so` files byte-identical to the working APK; DEX comparison found the supplied SDK JAR had only 3 `org.fmod` classes while the working APK had 7 and native strings required `AudioDevice`, `MediaCodec`, and `FMOD`. Restoring the complete 2.03.13 Java glue removed JNI error 28. Issue #78 separately identified a native null dereference for missing mod events. | The build now rejects an incomplete input JAR and also verifies the required descriptors survived into final DEX. Existing matched core/studio/Godot extension checks and missing-event guards remain. | Physical standalone launch proves FMOD loads through Godot/.NET with no JNI failure. Base-game audio and a supported mod bank after PLAY remain required. |
| Steamworks native dependency is absent on Android | ruled out | The Android path deliberately replaces platform initialization and skips Steam-backed mod enumeration; SteamKit2 supplies launcher network features. Collected successful launcher logs reach login/download without a Steamworks native load. | Keep critical platform/mod-loader patch target audit so a game update cannot silently reactivate the desktop path. | Physical online/offline launches and both supported branch transitions passed; the game's expected native-Steam warning did not prevent SteamKit2 cloud/depot handling or menu startup. |

## Baseline causality rules

- Treat the earliest error that changes control flow or process state as the causal candidate; record the terminal exit separately.
- `QueuePresentKHR`, `queueBuffer`, or swapchain errors after `onPause`, surface destruction, or process teardown are consequences unless an earlier first-frame failure proves otherwise.
- Absence of a managed exception in self-captured logcat is not evidence of a clean exit. Use the following boot's `ApplicationExitInfo`, system tombstone/native symbols where authorized, and heartbeat/lifecycle boundaries.
- A third-party failure is not marked fixed merely because all mods were disabled. The no-mod run establishes the launcher boundary; the smallest failing mod set establishes attribution.
